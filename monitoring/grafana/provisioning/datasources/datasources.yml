# Grafana datasource provisioning
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true

  - name: Fleet-API
    type: prometheus
    access: proxy
    url: http://host.docker.internal:5000/metrics
    editable: true
